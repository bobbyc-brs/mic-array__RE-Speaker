# ReSpeaker 4 Mic Array — runtime dependencies
#
# mic_array.py
pyusb==1.3.1
sounddevice==0.5.5
numpy==2.4.6

# doa_transcribe.py (adds to the above)
webrtcvad==2.0.10
whisper-timestamped==1.15.9
requests                 # HTTP forwarding to backend

# ai_client.py (adds to the above)
anthropic>=0.40.0           # Claude API (Anthropic provider)
openai>=1.0.0               # OpenAI-compat client (Perplexity, Cohere)
flask>=3.0.0                # HTTP server for ingest endpoint
flask-cors>=4.0.0           # CORS headers (optional, for browser clients)
pyyaml>=6.0                 # epcr.yaml schema loading

# Optional: pyrpc (internal package) for RPC forwarding
# pyrpc
